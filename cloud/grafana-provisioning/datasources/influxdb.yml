apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: gateway
      defaultBucket: telemetry
    secureJsonData:
      token: gateway-dev-token
    isDefault: true
